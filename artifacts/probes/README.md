# Source Probes

For the milestone, every primary data source should have a tiny proof-of-access artifact.

Good probe artifacts:

- one row from an API response
- one row from a CSV or Excel file
- one screenshot of a successful authenticated fetch
- one tiny JSON or Markdown note that shows the fetch worked

These are not full analyses. They are proof that the source is real and reachable.
